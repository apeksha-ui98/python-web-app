# Service for the application
apiVersion: v1
kind: Service
metadata:
  name: python-web-app
  labels:
    app: python-web-app
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: python-web-app
  type: ClusterIP